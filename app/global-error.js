// layout.js에서 에러 날 때 보여주는 파일

/* 'use-client'

export default function Error({error, reset}){
    return (
      <div>
        <h4>{error}</h4>
        <button onClick={()=>{ reset() }}>다시시도</button>
      </div>
    )
  } */